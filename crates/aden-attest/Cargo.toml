[package]
name = "aden-attest"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "EXPERIMENTAL: Attestation and provenance primitives (not yet integrated)"

[dependencies]
aden-core = { path = "../aden-core" }
serde = { version = "1.0", features = ["derive"] }

[package.metadata.aden]
status = "experimental"
integrated = false
notes = "Cryptographic attestation and provenance tracking. Planned for supply chain security features."
