[package]
name = "aden-telemetry"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "EXPERIMENTAL: Telemetry and token usage tracking (not yet integrated)"

[dependencies]
aden-core = { path = "../aden-core" }
serde = { version = "1.0", features = ["derive"] }

[package.metadata.aden]
status = "experimental"
integrated = false
notes = "Token savings calculation, latency tracking, and usage telemetry. Planned for performance monitoring."
