[package]
name = "aden-api"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "EXPERIMENTAL: REST/gRPC API types for aden (not yet integrated)"

[dependencies]
aden-core = { path = "../aden-core" }
aden-graph = { path = "../aden-graph" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[package.metadata.aden]
status = "experimental"
integrated = false
notes = "REST/gRPC types defined but not integrated into CLI or MCP. Planned for future server mode."
