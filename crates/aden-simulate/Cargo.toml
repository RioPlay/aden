[package]
name = "aden-simulate"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "EXPERIMENTAL: Contract simulation and threat modeling (not yet integrated)"

[dependencies]
aden-core = { path = "../aden-core" }
serde = { version = "1.0", features = ["derive"] }

[package.metadata.aden]
status = "experimental"
integrated = false
notes = "Contract diff simulation and threat model generation. Planned for adversarial testing features."
