:proj: aden
:author: RioPlay
:lang: rust
:toc: left
:toclevels: 3

[[index]]
= Aden Documentation Index

This is the canonical entry point for all Aden documentation. Read the document that matches your role.

== For New Users

|===
|Document |What You'll Learn |Time

|<<getting-started>> |Install, initialize, generate contracts, ask your first question |10 minutes
|<<user-guide>> |Daily workflows: onboarding, refactoring, code review, merge |30 minutes
|<<commands>> |Complete CLI reference with every flag and option |15 minutes
|===

== For AI Agents

|===
|Document |What You'll Learn |Time

|<<ai-integration>> |How to use `aden ask`, `aden asm`, `aden query`, `aden locate` as an AI agent |20 minutes
|link:mcp-intro.md[MCP Setup] |Install the Aden MCP server (`aden mcp install --platform <client>`) and verify it |10 minutes
|===

== For Contributors

|===
|Document |What You'll Learn |Time

|<<architecture>> |Crate architecture, data flow, pipeline stages |30 minutes
|<<project-context>> |Crate map, dependencies, glossary, conventions |20 minutes
|<<philosophy>> |Design rationale: why AsciiDoc, why a graph, why token density |20 minutes
|===

== Reference

|===
|Document |Purpose

|<<troubleshooting>> |Common problems and their fixes
|<<use-cases>> |Use cases beyond software engineering
|<<experimental-crates>> |Experimental and standalone crate status
|===

== Architecture Decisions

|===
|Document |Decision

|<<adr-001>> |All architecture decisions: AsciiDoc/Rust, AGPL-3.0, Markdown emission, polyglot toolchain, semantic networks
|===

== Quick Reference

=== Most Common Commands

[source,bash]
----
# Install (builds release; installs both `aden` and `aden-mcp` to ~/.local/bin)
./install.sh
# NOTE: `cargo install --path crates/aden-cli` installs ONLY the `aden` binary
# and silently omits the `aden-mcp` server. Use ./install.sh if you want MCP.

# Initialize a project
aden init

# Generate contracts from source
aden gen . --auto

# Validate the graph
aden check .

# Ask a question
aden ask "How does authentication work?"

# Detect drift
aden heal .

# Full CI gates
aden ci-check .
----

=== Command Categories

|===
|Category |Commands

|Core |`init`, `new`, `gen`, `check`
|Query |`ask`, `search`, `list`, `locate`, `asm`, `query`, `query-adq`
|Healing |`heal`, `review`, `sync`, `watch`
|CI/Security |`ci-check`, `audit`, `emergency`
|Tooling |`doctor`, `licenses`, `session`, `suggest`, `complete`
|Workspace |`federation`, `mcp`, `workflow`, `kickoff`
|===

== Related Documents

. <<getting-started>>
. <<user-guide>>
. <<commands>>
. <<ai-integration>>
. <<architecture>>
. <<project-context>>
. <<philosophy>>
. <<troubleshooting>>
. <<use-cases>>
. <<experimental-crates>>
