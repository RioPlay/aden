:proj: aden
:lang: rust
:ai_name: agent
:standard: cargo

[[agent-context]]
= Shared Context for Agent Sessions

This file is the canonical shared memory for all agent sessions working on `aden`.
Include it at the top of every prompt with `include::.agent/context.adoc[]`.

== Project Identity

[cols="1,2"]
|===
|Name |aden
|Language |rust
|AI Name |agent
|Standard |cargo workspace
|===

== Hard Constraints (Never Violate)
ifdef::agent[]

. **Never commit without `aden check` passing.**
. **Never edit contracts by hand — always regenerate with `aden gen`.**
. **Never emit files outside the working directory** without confirmation.
. **Never ignore test failures.**
. **Never commit `contracts/` or `.aden/`** (they are build artifacts).

endif::agent[]

== Language Defaults
ifdef::agent[]
- Primary language: `unknown`
- Edition / version: 2024
- Standard framework / runtime: unknown
endif::agent[]

== Dependencies
|===
|Name |Purpose |Version Policy

| | |
|===

== Next Steps

. Read `.agent/onboarding.adoc` before your first session.
. Read `.agent/constraints.adoc` to know what not to do.
. Append your session entry to `.agent/session.adoc` before starting work.

== Related Documents

. <<agent-onboarding>>
. <<agent-session>>
. <<security-audit-2026-05-24>>
. <<getting-started>>
. <<user-guide>>
. <<ai-integration>>
