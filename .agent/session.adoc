:proj: aden
:session: idle

[[agent-session]]
= Agent Session Log

This file is the canonical shared memory for parallel agent sessions.
Every agent that modifies files must append an entry here before completing.

== Active Sessions

|===
|Timestamp |Agent |Task |Files |Status |Confidence
|2026-07-22T22:10:00Z |pi-composer |Prepare scoped Aden 0.3.1 release metadata, repair draft-release repository targeting, and remediate inherited all-target clippy failures |Cargo.toml, Cargo.lock, CHANGELOG.md, docs/releasing.adoc, .github/workflows/release.yml, crates/aden-mcp/src/lib.rs, crates/aden-cli/tests/mcp_flag_parity.rs, crates/aden-cli/tests/coverage_dispositions.rs |complete |High
|===

== How to Append

[source,asciidoc]
----
|2026-05-24T14:30:00Z |agent-7 |Refactor parser |src/parser.rs |in_progress |High
----

Confidence: High (>0.9), Medium (0.7–0.9), Low (<0.7 — include blocker in task description).

== Invariants

. Read this file before every session.
. Append a row before declaring work complete.
. If two agents touch the same file, reconcile before committing.
. Never delete rows; only append.

== Related Documents

. <<agent-context>>
. <<agent-onboarding>>
